# Shared skill (test fixture)

This skill lives under `skills/test-shared-skill/` and is staged
into every runtime session via the shared skills mount.

Marker string for assertions: `PLATFORM_TEST_SHARED_SKILL_MARKER`.

Use this skill only when the prompt explicitly references it.
